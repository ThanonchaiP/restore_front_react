import { Typography } from "@mui/material"

type Props = {}

const Checkout = (props: Props) => {
  return <Typography>Only logged in users should be able to see this!</Typography>;
}

export default Checkout